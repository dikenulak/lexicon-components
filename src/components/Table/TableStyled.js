import styled from 'styled-components';

const TableStyled = styled.table`
  display: inline-block;
  font-weight: 600;
`;

const TableTitleStyled = styled.span`
  display: inline-block;
  font-weight: 600;
`;

const TableHeaderStyled = styled.span`
  display: inline-block;
  font-weight: 600;
`;

const TableFooterStyled = styled.span`
  display: inline-block;
  font-weight: 600;
`;

const TableBodyStyled = styled.span`
  display: inline-block;
  font-weight: 600;
`;

export {
  TableTitleStyled,
  TableHeaderStyled,
  TableBodyStyled,
  TableFooterStyled,
  TableStyled,
};

export default TableStyled;
